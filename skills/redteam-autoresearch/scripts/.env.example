# Copy this file to ../.env (the skill root) and fill in your key(s).
# The harness loads it via python-dotenv. Never commit your real .env.

# Default provider: OpenRouter -- one key, hundreds of models selected by id.
OPENROUTER_API_KEY=

# Alternative OpenAI-compatible providers (uncomment and set as needed).
# UBICLOUD_API_KEY=
# OPENAI_API_KEY=
